const scene=document.querySelector('#scene'),bouquet=document.querySelector('#bouquet'),modal=document.querySelector('#modal'),open=document.querySelector('#open'),close=document.querySelector('#close'),toast=document.querySelector('#toast');
let raf;function tilt(x,y){cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{bouquet.style.transform=`rotateX(${y}deg) rotateY(${x}deg) rotateZ(${x*.08}deg)`})}
scene.addEventListener('pointermove',e=>{const r=scene.getBoundingClientRect();tilt(((e.clientX-r.left)/r.width-.5)*16,-((e.clientY-r.top)/r.height-.5)*12)});scene.addEventListener('pointerleave',()=>bouquet.style.transform='');
function particles(x,y){for(let i=0;i<18;i++){const p=document.createElement('span');p.className='particle';p.textContent=i%3?'✦':'♡';p.style.cssText=`left:${x}px;top:${y}px;--x:${Math.random()*220-110}px;--y:${Math.random()*-220-30}px`;document.body.appendChild(p);setTimeout(()=>p.remove(),1200)}}
bouquet.addEventListener('click',e=>{particles(e.clientX,e.clientY);toast.textContent='Bir çiçek senin için açtı ✦';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1600)});
function show(){modal.classList.add('show');modal.setAttribute('aria-hidden','false');particles(innerWidth/2,innerHeight/2)}function hide(){modal.classList.remove('show');modal.setAttribute('aria-hidden','true')}
open.addEventListener('click',show);close.addEventListener('click',hide);modal.addEventListener('click',e=>{if(e.target===modal)hide()});document.addEventListener('keydown',e=>{if(e.key==='Escape')hide()});
